import { HandleValidatorDirective } from './handle-validator.directive';

describe('HandleValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
